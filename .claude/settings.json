{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_input.filePath // \"\"' | grep -q 'skiplist.json' && echo '{\"decision\":\"block\",\"reason\":\"skiplist.jsonを直接編集しないでください。python3 scripts/skiplist.py を使用してください。\"}' || echo '{}'"
          }
        ]
      }
    ]
  }
}
